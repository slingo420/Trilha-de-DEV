import { StyleSheet} from "react-native"

const styles = StyleSheet.create({
    formContext{
        width:"100%",
        height:"100%",
        bottom:0,
        backgroundColor:"ffffff",
        alignItens:"center",
        marginTop:30,
    },
});